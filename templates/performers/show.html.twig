{% extends 'base.html.twig' %}

{% block title %}Performers{% endblock %}

{% block body %}
    <h1>Performers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ performer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ performer.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ performer.picture }}</td>
            </tr>
            <tr>
                <th>Biography</th>
                <td>{{ performer.biography }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('performers_index') }}">back to list</a>

    <a href="{{ path('performers_edit', {'id': performer.id}) }}">edit</a>

    {{ include('performers/_delete_form.html.twig') }}
{% endblock %}
