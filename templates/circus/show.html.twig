{% extends 'base.html.twig' %}

{% block title %}Circus{% endblock %}

{% block body %}
    <h1>Circus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circus.id }}</td>
            </tr>
            <tr>
                <th>History</th>
                <td>{{ circus.history }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('circus_index') }}">back to list</a>

    <a href="{{ path('circus_edit', {'id': circus.id}) }}">edit</a>

    {{ include('circus/_delete_form.html.twig') }}
{% endblock %}
